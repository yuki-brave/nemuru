  <h5 class="card-header"><%= link_to tweet.title, tweet_path(tweet.id) %>（投稿者：<%= link_to tweet.user.nickname, user_path(tweet.user.id) %>） #<%= tweet.tags&.first&.tag_name %></h5>
  <div class="card-body">
    気持ち： <%= Mind.find(tweet.mind_id)[:name] %>　カテゴリー：<%= Category.find(tweet.category_id)[:name] %>　時間帯：<%= TimeZone.find(tweet.time_zone_id)[:name] %>
  </div>